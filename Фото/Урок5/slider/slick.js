$(document).ready(function(){
    $('.content__slider').slick({
        dots:true
    });
    $('.posts__slider').slick({
        
        slidesToShow:3,
        centerPadding: '30px',
        centerMode:true,
    });
});